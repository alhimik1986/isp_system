import { AjaxTooltipsterDirective } from './ajax-tooltipster.directive';

describe('AjaxTooltipsterDirective', () => {
  it('should create an instance', () => {
    const directive = new AjaxTooltipsterDirective();
    expect(directive).toBeTruthy();
  });
});
